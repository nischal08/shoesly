enum RequestType {
  get,
  getWithToken,
  post,
  postWithToken,
  postWithHeaders,
  postWithOnlyHeaders,
  delete,
deleteWithToken,
  putWithHeaders
}

// ignore: constant_identifier_names
enum Status { INITIAL, LOADING, COMPLETED, ERROR }
